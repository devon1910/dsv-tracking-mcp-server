# Phase 6 Verification

This document requires live browser verification against the real DSV tracking API.
Run `make verify` (or `go run ./cmd/dsv-verify/`) after building to populate the results below.

The table below will be filled in after a live run confirms the full stack end-to-end.

| Fixture / STT | Tool | Field | Expected | Actual | Pass? |
|---------------|------|-------|----------|--------|-------|
| (run make verify to populate) | | | | | |
